/**
 * Created by n_andrade on 9/25/2017.
 */
'use strict'

/*
1-Subscribe to /device/announcement in MQTT
2-When a new device appears, add it to the list of discovered devices.

3-Add functionality to save the values in a config file (mac address, ip, associated alias, features)
4-Add feature to load these values from JSON as well.


Features:
    - Write to device:
        - Light values
    - Receive from device
        - Buttons (short press, long press, double press)
        - Knobs (values)
 */
